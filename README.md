# Kinoshka

Настольное приложение для просмотра видео с YouTube, фильмов и сериалов с RuTube
и прослушивания музыки — онлайн и офлайн, с персональными рекомендациями,
локальными профилями и встроенным VPN для обхода блокировок.

Разработано **Budin's industries**.

---

## Что уже работает

| Возможность | Состояние |
|---|---|
| Локальные профили: первый запуск, вход, пароль, смена аккаунта | ✅ готово |
| Интересы и рекомендации (просмотры + поиск + затухание) | ✅ готово |
| YouTube: лента рекомендаций, тренды, поиск, похожее | ✅ готово |
| Плеер с точным учётом просмотра | ✅ готово |
| Скачивание видео и аудио, очередь с прогрессом | ✅ готово |
| Библиотека «Мои видео / фильмы / музыка», офлайн-просмотр | ✅ готово |
| История с фильтрами и группировкой по дням | ✅ готово |
| VPN WireGuard: импорт .conf, подключение, ротация, прокси | ✅ готово |
| Панель настроек | ✅ готово |
| Адаптивный интерфейс (сворачивание меню) | ✅ готово |
| Сборка .exe + установщик | ✅ готово |
| RuTube: поиск, тренды, воспроизведение | ✅ готово |
| Запасной поиск по интернету, если фильма нет на RuTube | ✅ готово |
| Кинопоиск: рейтинги, постеры, годы, жанры | ✅ готово |
| ИИ-поиск фильмов (+ работа без ключа) | ✅ готово |
| Музыка: поиск, рекомендации, скачивание в mp3 | ✅ готово |
| Джарвис | 🚧 следующий этап |

---

## Быстрый старт

```bash
python -m venv .venv
.venv\Scripts\activate          # Windows
# source .venv/bin/activate     # Linux/macOS

pip install -r requirements.txt

python main.py                  # окно приложения
python main.py --web            # то же самое в браузере (отладка)
python main.py -v               # подробные логи
```

Дополнительно нужен **ffmpeg** в PATH — без него нельзя склеивать видео
высокого качества с аудиодорожкой и конвертировать музыку в mp3.

---

## VPN для YouTube

В России YouTube не открывается напрямую, поэтому VPN встроен в приложение.

Поддерживаются **два типа** туннелей, приложение различает их автоматически:

| Тип | Признак в файле | Чем поднимается |
|---|---|---|
| WireGuard | обычные `[Interface]` / `[Peer]` | `wg-quick` / `wireguard.exe` |
| AmneziaWG | параметры `Jc`, `Jmin`, `S1…S4`, `H1…H4`, `I1` | `awg-quick` / `amneziawg.exe` |

> [!IMPORTANT]
> AmneziaWG — форк WireGuard с обфускацией трафика. Обычный `wg-quick`
> падает на его конфигурациях с ошибкой «Unrecognized key», поэтому для них
> нужен клиент с [amnezia.org](https://amnezia.org). Тип туннеля виден
> в списке конфигураций в настройках.

1. Откройте **Настройки → VPN для YouTube**.
2. Нажмите **«Импортировать .conf»** и выберите свои файлы.
3. Включите переключатель **«Использовать VPN»** и нажмите **«Подключить»**.
4. Кнопка **«Проверить соединение»** покажет, доступен ли YouTube и ваш внешний IP.

Как это работает:

* конфигурации копируются в `%APPDATA%\Kinoshka\vpn` — **не в репозиторий**,
  потому что внутри приватные ключи (`vpn/` и `*.conf` в `.gitignore`);
* перед каждым запросом к YouTube вызывается `VpnManager.ensure_connected()`;
* если конкретный узел заблокирован, `YouTubeProvider` автоматически пробует
  следующую конфигурацию (`vpn.rotate()`);
* для системного туннеля нужен установленный клиент (WireGuard или
  AmneziaWG — смотрите таблицу выше) и права администратора;
* если ставить WireGuard нельзя — впишите HTTP/SOCKS-прокси в настройках,
  он будет использован и запросами, и yt-dlp.

---

## Структура проекта

```
config.py                  Пути, настройки (JSON), значения по умолчанию
main.py                    Точка входа (десктоп / --web)
app.py                     Роутер: шапка, меню, навигация, общие действия
build_installer.py         Сборка .exe (PyInstaller) и установщика (Inno Setup)

core/
  media.py                 MediaItem — единый формат контента для всех источников
  session.py               AppSession — текущий профиль и доступ ко всем сервисам
  vpn.py                   VpnManager — импорт .conf, подъём туннеля, прокси
  downloader.py            DownloadManager — очередь загрузок с прогрессом
  profile_service.py       Профили: создание, вход, смена, удаление
  recomendation_engine.py  Интересы, история, персонализация выдачи
  ai_search.py             Разбор запросов на естественном языке (+ эвристика)
  providers/
    base.py                Интерфейс источника контента
    youtube.py             YouTube через yt-dlp
    rutube.py              RuTube: поиск через JSON API, поток через yt-dlp
    web.py                 Запасной поиск видео по интернету
    kinopoisk.py           Рейтинги, постеры, жанры (kinopoisk.dev)
    film.py                Агрегатор: RuTube + интернет + Кинопоиск + ИИ
    music.py               Музыка через YouTube Music

database/
  __init__.py              engine, сессии, init_db()
  models/                  User, History, Collection, UserInterest,
                           SearchHistory, VideoMetadata

UI/
  themes/DarkTheme.py      Палитра, градиенты, тема
  components/              AppBar, Navigator, MediaCard, кнопки, состояния
  views/                   Экраны приложения

tests/smoke_ui.py          Дымовой тест: строит все экраны без браузера
```

### Где лежат пользовательские данные

| ОС | Путь |
|---|---|
| Windows | `%APPDATA%\Kinoshka` |
| Linux | `~/.local/share/kinoshka` |
| macOS | `~/Library/Application Support/Kinoshka` |

Там же база `kinoshka.db`, `settings.json`, `vpn/`, `downloads/`, `logs/`.
Переменная `KINOSHKA_DATA_DIR` переопределяет каталог — удобно для тестов
и портативного режима.

---

## Как работают рекомендации

Профиль вкусов — это набор пар «категория → вес» (0.1 … 5.0):

* **просмотр**: вес растёт тем сильнее, чем больше досмотрено
  (досмотрел до конца +0.30, бросил в начале +0.05);
* **поиск**: поисковая фраза добавляется как интерес (+0.25), совпавшие
  категории усиливаются (+0.15);
* **затухание**: интересы, которые месяц не подтверждались, слабеют на 10%
  за сеанс — лента не застревает на прошлогодних увлечениях.

Движок не ходит в сеть. Он отдаёт `build_query_seeds()` — список запросов из
топовых интересов и недавних поисков, по ним провайдер собирает ленту, а затем
`personalize()` пересортировывает результат и убирает уже досмотренное.

Учёт просмотра идёт по событиям позиции плеера, а не по таймеру: пауза
не засчитывается, перемотка вперёд не накручивает время.

---

## Сборка установщика (Windows)

```bash
pip install pyinstaller
# и Inno Setup 6: https://jrsoftware.org/isdl.php

python build_installer.py
```

Результат: `installer_output\Kinoshka-Setup-0.2.0.exe`.

Другие режимы:

```bash
python build_installer.py --onefile       # один .exe, без установщика
python build_installer.py --no-installer  # только сборка
python build_installer.py --console       # оставить консоль для отладки
```

Положите `ffmpeg.exe` в корень проекта перед сборкой — он попадёт внутрь
дистрибутива, и пользователю не придётся ставить его отдельно.
Иконка берётся из `assets/icon.ico`, если файл есть.

Установщик намеренно **не удаляет** `%APPDATA%\Kinoshka` при деинсталляции:
профили, история и скачанное переживают переустановку.

---

## Тесты

```bash
KINOSHKA_DATA_DIR=/tmp/kinoshka-test python tests/smoke_ui.py
```

Тест строит все экраны и компоненты со заглушкой страницы. Он не требует
браузера и сети и ловит несовместимости с API Flet (переименованные параметры
и события) — именно так были найдены отличия Flet 0.86: `PopupMenuItem.content`
вместо `text`, `Dropdown.on_select` вместо `on_change`, асинхронный
`FilePicker.pick_files()`, события `Video.on_load` / `on_complete`.

---

## Как ищутся фильмы и сериалы

Порядок работы `FilmProvider` — ровно как в техническом задании:

1. **Разбор запроса.** `ai_search.parse()` превращает фразу
   «комедия про роботов 2020 с высоким рейтингом» в поисковые запросы плюс
   фильтры (жанр, годы, рейтинг, фильм/сериал). С ключом OpenAI это делает
   модель, без ключа — эвристика по ключевым словам. **Поиск работает всегда**,
   наличие ключа лишь повышает точность.
2. **RuTube** — основной источник просмотра, поиск через публичный JSON API
   (у yt-dlp нет экстрактора поиска для RuTube, только воспроизведение).
3. **Запасной поиск по интернету** — включается автоматически, если на RuTube
   пусто: DuckDuckGo → отбор доменов → проверка каждой ссылки через yt-dlp.
   В интерфейсе такие результаты помечаются отдельной плашкой.
4. **Кинопоиск** дополняет карточки рейтингом, постером, годом и жанрами
   (параллельно, в 4 потока). Смотреть с Кинопоиска нельзя — если открыть
   его карточку, приложение само найдёт фильм на RuTube.

Фильтры применяются мягко: если после них ничего не осталось, лучше показать
менее точный результат, чем пустой экран.

## Ключи API (все необязательные)

| Настройка | Где взять | Что даёт | Без него |
|---|---|---|---|
| `ai_api_key` | OpenAI-совместимый сервис | Понимание запросов на естественном языке | Эвристический разбор |
| `kinopoisk_api_key` | [kinopoisk.dev](https://kinopoisk.dev) (бесплатный тариф) | Рейтинги, постеры, годы, жанры, новинки | Данные с RuTube как есть |

## Планы на следующий этап

1. **Джарвис** — чат-ассистент и голосовые команды.
2. **Аудиоплеер** с очередью, перемешиванием и плейлистами.
3. **Отслеживание серий** — «продолжить с 4 серии 2 сезона».
